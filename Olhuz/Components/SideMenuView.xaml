<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:models="clr-namespace:Olhuz.Models"
    x:Class="Olhuz.Components.SideMenuView"
    x:Name="MenuView"
    BackgroundColor="{StaticResource OneBlue}">

    <!-- Conte�do principal -->
    <Grid RowDefinitions="2*, 8.5*">
        <!-- Cabe�alho (Linha 0) -->
        <HorizontalStackLayout
            Grid.Row="0"
            HorizontalOptions="Center"
            VerticalOptions="Center"
            Spacing="14"
            Margin="0,0,10,0"
            MinimumHeightRequest="60"
            Padding="0">

            <Image
                Source="logo.png"
                Aspect="AspectFit"
                MaximumWidthRequest="60"
                VerticalOptions="Center"/>

            <Label
                Text="Olhuz"
                FontSize="40"
                VerticalOptions="Center"/>

        </HorizontalStackLayout>

        <!-- Menu (Linha 1) -->
        <Border
            Grid.Row="1"
            BackgroundColor="{StaticResource TwoBlue}"
            StrokeShape="RoundRectangle 0 200 0 0"
            StrokeThickness="0"
            Shadow="{x:Null}"
            HorizontalOptions="Fill">

            <VerticalStackLayout VerticalOptions="Center" Padding="0">
                <!-- ADICIONADO x:Name para o Controller se referir � lista de itens -->
                <VerticalStackLayout x:Name="MenuStackLayout"
                                     BindableLayout.ItemsSource="{Binding Items}">
                    <BindableLayout.ItemTemplate>
                        <DataTemplate x:DataType="models:MenuItemModel">

                            <Border
                                x:Name="MenuOptionBorder"
                                StrokeThickness="0"
                                StrokeShape="RoundRectangle 40 0 40 0"
                                Padding="20, 20"
                                Margin="10, 4, 0,4"
                                Shadow="{x:Null}">

                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal" />
                                        <VisualState x:Name="PointerOver">
                                            <VisualState.Setters>
                                                <Setter Property="BackgroundColor" Value="{StaticResource FourBlue}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>

                                <Border.Triggers>
                                    <!-- A propriedade IsSelected do Model (MenuItemModel) ainda controla o visual -->
                                    <DataTrigger TargetType="Border" Binding="{Binding IsSelected}" Value="True">
                                        <Setter Property="BackgroundColor" Value="{StaticResource OneBlue}" />
                                    </DataTrigger>

                                    <DataTrigger TargetType="Border" Binding="{Binding IsSelected}" Value="False">
                                        <Setter Property="BackgroundColor" Value="Transparent" />
                                    </DataTrigger>
                                </Border.Triggers>

                                <Border.GestureRecognizers>
                                    <!-- Evento Tapped que ser� tratado no Code-Behind (Controller) -->
                                    <TapGestureRecognizer Tapped="OnMenuItemTapped"/>
                                </Border.GestureRecognizers>

                                <HorizontalStackLayout Spacing="24">
                                    <Image
                                        Source="{Binding Icon}"
                                        HeightRequest="30"
                                        Aspect="AspectFit"/>
                                    <Label
                                        Text="{Binding Title}"
                                        TextColor="Black"
                                        FontSize="24"
                                        VerticalOptions="Center"/>
                                </HorizontalStackLayout>

                            </Border>
                        </DataTemplate>
                    </BindableLayout.ItemTemplate>
                </VerticalStackLayout>
            </VerticalStackLayout>
        </Border>
    </Grid>
</ContentView>